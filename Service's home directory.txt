root's home directory is /root
daemon's home directory is /usr/sbin
bin's home directory is /bin
sys's home directory is /dev
sync's home directory is /bin
games's home directory is /usr/games
man's home directory is /var/cache/man
lp's home directory is /var/spool/lpd
mail's home directory is /var/mail
news's home directory is /var/spool/news
uucp's home directory is /var/spool/uucp
proxy's home directory is /bin
www-data's home directory is /var/www
backup's home directory is /var/backups
list's home directory is /var/list
irc's home directory is /run/ircd
gnats's home directory is /var/lib/gnats
nobody's home directory is /nonexistent
systemd-network's home directory is /run/systemd
systemd-resolve's home directory is /run/systemd
messagebus's home directory is /nonexistent
systemd-timesync's home directory is /run/systemd
syslog's home directory is /home/syslog
_apt's home directory is /nonexistent
_chrony's home directory is /var/lib/chrony
nm-openvpn's home directory is /var/lib/openvpn/chroot
colord's home directory is /var/lib/colord
speech-dispatcher's home directory is /run/speech-dispatcher
geoclue's home directory is /var/lib/geoclue
gdm's home directory is /var/lib/gdm3
gnome-initial-setup's home directory is /run/gnome-initial-setup/
tss's home directory is /var/lib/tpm
tcpdump's home directory is /nonexistent
rtkit's home directory is /proc
whoopsie's home directory is /nonexistent
usbmux's home directory is /var/lib/usbmux
cups-pk-helper's home directory is /home/cups-pk-helper
avahi-autoipd's home directory is /var/lib/avahi-autoipd
_flatpak's home directory is /nonexistent
saned's home directory is /var/lib/saned
dnsmasq's home directory is /var/lib/misc
avahi's home directory is /run/avahi-daemon
fwupd-refresh's home directory is /run/systemd
uuidd's home directory is /run/uuidd
dagmawi's home directory is /home/dagmawi
postgres's home directory is /opt/ManageEngine/OpManager/pgsql
